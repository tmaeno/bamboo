version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:v1.18.3
    ports:
      - "6333:6333"  # HTTP
      - "6334:6334"  # gRPC
    volumes:
      - qdrant_data:/vector_db/storage
    networks:
      - bamboo-network

volumes:
  qdrant_data:

networks:
  bamboo-network:
    driver: bridge

