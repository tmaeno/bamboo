"""Code Map — a machine-derived view of a target system's source.

Answers "which code settles this value, under what conditions, and where does
it hand off to another system".  Built offline per source version, queried at
reasoning time; see :mod:`bamboo.codemap.base` for the plugin contract.
"""
